import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function ConfiguracoesPage() {
  return (
    <PlaceholderPage
      title="Configurações"
      description="Perfis de usuário, integrações (Vindi, ZapSign, Drive, WhatsApp), templates de contrato e parâmetros da clínica."
      phase="Implementado ao longo das fases"
    />
  );
}
